{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: "Category Group"|t('sprout-base-import'),
    instructions: "The category group to import content."|t('sprout-base-import'),
    required: true,
    name: "categoryGroup",
    options: [{label: "Select a group..."|t('sprout-base-import'), value: null}]|merge(categoryGroups),
    value: seedJob.settings.categoryGroup ?? null,
    errors: seedJob.getErrors('categoryGroup') ?? null
}) }}
