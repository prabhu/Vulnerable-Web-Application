{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Placeholder Text"|t('sprout-base-fields'),
    instructions: "The text that will be shown if the field doesn’t have a value."|t('sprout-base-fields'),
    id: 'placeholder',
    name: 'placeholder',
    value: field.placeholder
}) }}

<div id="sproutregularexpressionfield-custom-validation-pattern">
    {{ forms.textField({
        label: 'Validation Pattern'|t('sprout-base-fields'),
        instructions: 'The regular expression to use to validate this field.'|t('sprout-base-fields'),
        name: 'customPattern',
        placeholder: '(.*)',
        value: field.customPattern,
        errors: field.getErrors('customPattern')
    }) }}

    {{ forms.textField({
        label: 'Error Message'|t('sprout-base-fields'),
        instructions: 'Display this error message when validation fails.'|t('sprout-base-fields'),
        name: 'customPatternErrorMessage',
        placeholder: 'Invalid format.'|t('sprout-base-fields'),
        value: field.customPatternErrorMessage
    }) }}
</div>

