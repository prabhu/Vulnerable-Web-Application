{% import "_includes/forms" as forms %}

{% set options = craft.sproutFields.getCountries() %}

{{ forms.selectField({
    name: 'country',
    instructions: "This country will display as selected when the field loads for the first time."|t('sprout-base-fields'),
    label: 'Default Country'|t('sprout-base-fields'),
    options: options,
    value: field.country
}) }}

{{ forms.lightswitchField({
    label: "Limit to single country"|t('sprout-base-fields'),
    instructions: "Only accept numbers from your Default Country"|t('sprout-base-fields'),
    name: 'limitToSingleCountry',
    on: field.limitToSingleCountry
}) }}

{{ forms.textField({
    label: "Placeholder Text"|t('sprout-base-fields'),
    instructions: "The text that will be shown if the field doesn’t have a value."|t('sprout-base-fields'),
    id: 'placeholder',
    name: 'placeholder',
    value: field.placeholder
}) }}

{{ forms.textField({
    label: 'Error Message'|t('sprout-base-fields'),
    instructions: 'Display this error message when a phone number is invalid.'|t('sprout-base-fields'),
    name: 'customPatternErrorMessage',
    placeholder: 'Phone number must match the format: ##########'|t('sprout-base-fields'),
    value: field.customPatternErrorMessage
}) }}